// Function to redirect the user to the membership page with a specific membership ID
function subscribe(id){
    window.location.href = "/memberships/" + id;  // Redirects to the URL with the given membership ID
}
